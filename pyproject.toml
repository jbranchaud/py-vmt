[project]
name = "py-vmt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "click>=8.3.1",
    "platformdirs>=4.9.4",
]

[project.scripts]
vmt = "py_vmt.cli:cli"
